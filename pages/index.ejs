<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Note</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
<% if (created) { %>
    <div class="alert alert-success">
        Note has been created!
    </div>
<% } %>
<% if(error) { %>
    <div class="alert alert-danger">
        Something was wrong!
    </div>
<% } %>
  <div class="container">
    <h1 class="text-center" >Notes</h1>
      <form class="form-container d-flex  justify-content-center" method="POST" action="/">
          <input type="text" class="form-control m-2" name="title">
          <button class="btn btn-primary" type="submit">Отправить</button>
      </form>
      <% if(notes.length) { %>
      <div class="card m-2">
              <ul class="list-group ">
                  <%for (let i = 0; i < notes.length; i++) { %>
                      <li class="list-group-item d-flex justify-content-between">
                          <%= notes[i].title%>
                          <div class="container-buttons">
                              <button class="btn btn-warning"  data-id="<%=notes[i].id%>" data-type="edit">✎</button>
                              <button class="btn btn-danger" data-id="<%=notes[i].id%>" data-type="remove">&times;</button>
                          </div>
                      </li>
                  <% } %>
              </ul>
      </div>
      <% } else {%>
          List is empty!
      <% }%>
  </div>
        <script src="/app.js"></script>
</body>
</html>